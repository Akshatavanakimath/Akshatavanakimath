#include<stdio.h>
void man()
{
  int x, i=1, count=0;
  printf("Enter the value of a:");
  scanf("%d",&x);
  while(i!=0)
  {
    {
      if(i%2==1)
        if(count!=x)
        {
          printf("%d\n",i);
          count++;
        }
    }
    i++;
  }
}
